{
  "name": "server",
  "version": "1.0.0",
  "main": "server.js",
  "license": "MIT",
  "scripts": {
    "start": "node server.js",
    "heroku-postbuild": "cd chat_client && npm install --only=dev && npm install && npm run build"
  },
  "dependencies": {
    "graphql-yoga": "^1.18.3"
  }
}
